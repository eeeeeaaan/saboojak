spring:
  application:
    name: individual project
  datasource:
    url: jdbc:mysql://localhost:3306/individual
    username: root
    password: "#helloworld!24"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379


jwt:
  secret: ${JWT_SECRET}
server:
  port: 8080
